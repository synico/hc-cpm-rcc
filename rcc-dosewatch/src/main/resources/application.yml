camel:
    springboot:
        main-run-controller: true
#        xml-routes: classpath:camel/*.xml,classpath:camel/*/*.xml
        xml-routes: classpath:camel/*.xml

spring:
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                show_sql: false
                connection:
                    charSet: UTF-8
                    useUnicode: true
                dialect: org.hibernate.dialect.PostgreSQL95Dialect
                temp:
                    use_jdbc_metadata_defaults: false

#Datasource for JPA to update database schema
custom:
    datasource:
        primary:
            url: jdbc:postgresql://192.168.62.79:5432/rcc_cpm_v0801
            driverClassName: org.postgresql.Driver
            username: postgres
            password: GehcDi9it@l
            connectionTimeout: 30000
            idleTimeout: 60000
            maxLifetime: 180000
            maximumPoolSize: 8
